\documentclass[11pt]{article}
\usepackage{booktabs}
\usepackage[margin=0.8in]{geometry}
\usepackage{graphicx}
\usepackage{url}

\begin{document}
\sloppy

\section*{Sphere-into-Corner Solver Performance}

This report summarizes the unified current-code, output-enabled
sphere-into-corner response matrix run on \texttt{rzwhippet47} on 2026-09-15.
The authoritative matrix is
\path{/usr/workspace/tupek2/dev/buckling_solver_paper/results/model_selection_rerun_20260915_095548/sphere_into_corner}.
The targeted AMG trust-region completion runs are
\path{/usr/workspace/tupek2/dev/buckling_solver_paper/results/model_selection_rerun_20260915_095548/sphere_into_corner_cap_40000}.
Together they retain commands, logs, response histories, executable identity,
status, and validation data.  The completion-run executable SHA-256 is
\texttt{dcaaa6a54f0306affafd7911a0075110bb93d147132f490bfce635b66bceefe6}.

\section*{Configuration}

The three-dimensional model uses the first-order
\path{hollow_sphere_octant_corner.g} mesh.  It is a hollow spherical-octant
shell with no essential displacement boundary conditions.  The material is
Neo-Hookean with density 1, Young's modulus 6, and Poisson ratio 0.33.  A load
patch is selected from the upper 24\% of the outer-surface projection range in
the direction opposite $(-1,-1,-1)$.  A traction drives this patch toward the
three coordinate planes over 16 equal increments from time zero to one.  The
peak traction magnitude is 0.019.

The coordinate planes are represented by a custom smoothed penalty boundary
integral on the outer surface.  The penalty parameter is 8, the nominal element
size is 0.025, and the smoothing band is $2.5\times10^{-5}$.  This is not a
Tribol mortar-contact calculation.  Before penalty contact constrains the
shell, the quasistatic tangent is intentionally under-constrained; the case
therefore stresses a singular-to-constrained transition.

All eight entries use eight MPI ranks, explicit cold starts, CG, nonlinear
tolerance $10^{-11}$ and linear tolerance $10^{-14}$. The initial matrix used a
10000-iteration nonlinear cap; the targeted AMG trust-region reruns use 40000.
Hypre AMG uses a 600-iteration CG cap, and Hypre Jacobi uses a
60000-iteration cap.  Both BSR SpMV and assembled BSR are disabled.  Every
entry writes the initial field state and each accepted increment.

\section*{Completion and Timing}

Both baseline and safeguarded-subspace trust-region methods now complete all 16
increments with either preconditioner. Both Newton line-search entries time out
without an accepted increment, and both PETSc Newton critical-point entries
fail on the first increment. At the old 10000 cap, baseline trust region with
AMG accepted 12 increments and subspace trust region with AMG accepted 14. At
40000, they complete with maximum per-increment nonlinear counts of 14716 and
12012, respectively. Every stored response state has a corresponding
visualization cycle.

The table reports challenge-suite max-rank solver wall time when the executable
returns.  The Newton-LS values are the outer 1800-second time limits because
those entries do not return normally.

\begin{table}[h]
  \centering
  \small
  \caption{Sphere-into-corner completion, nonlinear work, and max-rank wall time.}
  \begin{tabular}{@{}llll@{}}
    \toprule
    Solver & Preconditioner & Outcome & Iterations; time \\
    \midrule
    Newton-LS & Hypre AMG & $0/16$ steps, timeout & ---; $1801\,\mathrm{s}$ \\
    Newton-LS & Hypre Jacobi & $0/16$ steps, timeout & ---; $1800\,\mathrm{s}$ \\
    Newton-CP & Hypre AMG & step-1 failure & 1; $0.183\,\mathrm{s}$ \\
    Newton-CP & Hypre Jacobi & step-1 failure & 0; $0.618\,\mathrm{s}$ \\
    Trust-region & Hypre AMG & $16/16$ steps & 132699; $1456.674\,\mathrm{s}$ \\
    Trust-region & Hypre Jacobi & $16/16$ steps & 2442; $6.468\,\mathrm{s}$ \\
    Trust-region + subspace & Hypre AMG & $16/16$ steps & 100024; $564.050\,\mathrm{s}$ \\
    Trust-region + subspace & Hypre Jacobi & $16/16$ steps & 2099; $5.899\,\mathrm{s}$ \\
    \bottomrule
  \end{tabular}
\end{table}

At the larger AMG cap, subspace refinement reduces reported nonlinear work by
24.6\% and wall time by 61.3\% relative to baseline trust region. The Jacobi
rows retain the original 10000-cap measurements. These are single-run
measurements on a strongly
preconditioner-sensitive problem and do not establish a general performance
advantage.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.84\textwidth]{/usr/workspace/tupek2/dev/buckling_solver_paper/figures/sphere_into_corner_solver_timing.png}
  \caption{Log-scale wall time and outcome for the eight sphere-into-corner configurations.}
\end{figure}

\section*{Response Validation}

All accepted trust-region histories coincide at the precision written to CSV.
All four now reach time one and finish at signed drive displacement
$-0.0210414$ and load resultant 0.0265078, with 17 finite response states and
17 field cycles each.

The common partial path shows that the AMG failures do not precede a distinct
accepted branch in the recorded response quantity.  The comparison instead
isolates strong sensitivity of the nonlinear work to preconditioning during the
singular-to-constrained transition.

\begin{figure}[h]
  \centering
  \begin{minipage}{0.49\textwidth}
    \centering
    \includegraphics[width=\linewidth]{/usr/workspace/tupek2/dev/buckling_solver_paper/figures/sphere_into_corner_solver_response_HypreAMG.png}
  \end{minipage}
  \begin{minipage}{0.49\textwidth}
    \centering
    \includegraphics[width=\linewidth]{/usr/workspace/tupek2/dev/buckling_solver_paper/figures/sphere_into_corner_solver_response_HypreJacobi.png}
  \end{minipage}
  \caption{Applied-resultant versus load-patch drive-displacement histories.  Partial and completed trust-region histories overlay on their shared accepted states.}
\end{figure}

\section*{Solver Diagnostics}

Both PETSc Newton-CP entries report convergence reason $-4$ on the first
increment after their inner CG solve reaches its configured cap.  Newton-LS
encounters indefinite tangents repeatedly---123 diagnostics with AMG and 53
with Jacobi---then reports a nonfinite initial residual without producing an
accepted state.  The outer jobs are retained as timeouts because they do not
terminate before the imposed limit.

The old AMG trust-region failures had a different signature. No indefinite-CG
diagnostic or inner CG cap was reported; each stopped only because an increment
reached the 10000 nonlinear limit. Raising that cap to 40000 lets both complete,
confirming that the old failures were premature cap terminations rather than a
different accepted branch. With Jacobi, the largest per-increment nonlinear
counts remain 336 and 295.

\section*{Field Comparison}

The targeted September completion run contains all 17 field states.  The compact
figure retains the initial and final states of subspace trust region with Hypre
AMG.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.92\textwidth]{/usr/workspace/tupek2/dev/buckling_solver_paper/figures/sphere_into_corner_initial_final.png}
  \caption{Initial and final September sphere configurations for subspace trust region with Hypre AMG. Colors show displacement magnitude on the physical configuration without displacement amplification.}
\end{figure}

\section*{Interpretation and Limitations}

This matrix supports a narrow robustness conclusion: all four trust-region
configurations complete the singular-to-constrained loading path when the AMG
cases receive a sufficiently large nonlinear budget. Preconditioner choice
still dominates cost: AMG requires roughly $10^5$ accumulated nonlinear
iterations here, whereas the Jacobi runs need only a few thousand.

The case does not certify stability of the accepted states, validate a contact
formulation, or establish penetration accuracy.  It has no analytic reference,
accepted-state eigenanalysis, contact-gap study, load-step study, or penalty
sensitivity study.  It should therefore appear in the paper only as a
contact-like robustness and conditioning example, not as evidence for contact
accuracy or as the primary stable-equilibrium result.

\end{document}
