\documentclass[11pt]{article}
\usepackage{booktabs}
\usepackage[margin=0.8in]{geometry}
\usepackage{graphicx}
\usepackage{url}

\begin{document}
\sloppy

\section*{Viscoelastic Buckling and Recovery Solver Performance}

This report summarizes the unified current-code, output-enabled viscoelastic
response matrix run on \texttt{rzwhippet56} on 2026-09-17.  All eight entries and
their field output are contained in
\path{/usr/workspace/tupek2/dev/buckling_solver_paper/results/model_selection_rerun_20260915_095548/viscoelastic_buckling/peak_0p6_full}.
The compact provenance archive is
\path{/usr/workspace/tupek2/dev/buckling_solver_paper/results/model_selection_rerun_20260915_095548/viscoelastic_buckling/archive/solver_response_20260917_083605}.

\section*{Configuration}

The three-dimensional model uses the 1097-element hemispherical-cap mesh
\path{data/meshes/cap_hemisphere.g} with quadratic displacement elements.  The
bottom boundary is fixed, and the two symmetry boundaries constrain the $x$ and
$z$ displacement components.  A scalar temperature field is fixed at 300.  The
single-branch finite-deformation viscoelastic material has density $\rho=1$,
equilibrium shear modulus $G_\infty=10^3$, branch modulus
$G_0=3G_\infty$, glassy Poisson ratio $\nu_g=0.45$, relaxation time
$\tau_0=1$, and viscosity $\eta_0=G_0\tau_0=3000$.  The glassy shear
modulus is $G_g=G_\infty+G_0=4000$, and the bulk modulus computed from
$G_g$ and $\nu_g$ is $K=38666.7$.  Thermal expansion is disabled;
$\theta_r=\theta_{sf}=300$, $C_1=0$, and $C_2=50$.

The top patch is loaded downward with a force-controlled sawtooth history.  The
nominal force rises to 0.6 over $0\leq t\leq6$ and is removed on the next
increment.  Eighty time steps of size 0.3 advance the model to $t=24$, so an
accepted state records the full force 0.6 at $t=6$.  The loaded patch area
reported by the run is 0.0764072134.  All runs use 16 MPI ranks, explicit cold starts, CG,
nonlinear relative and absolute tolerances $10^{-11}$, and linear relative and
absolute tolerances $10^{-14}$.  Hypre AMG has a 600-iteration cap and Hypre
Jacobi has a 60000-iteration cap.  Every command explicitly contains
\texttt{--no-use-bsr-spmv} and \texttt{--no-assemble-bsr}; no BSR matrix or BSR
SpMV path is used.  Every configuration writes the initial field state and every
accepted state in ParaView format.

The eight configurations combine Newton line search, PETSc Newton critical-point
line search, baseline trust region, and safeguarded trust-region subspace
globalization with both preconditioners.  All logs identify Smith version
\texttt{v0.1.0-9d4024ad9}; the executable SHA-256 checksum is
\texttt{7cb85ea4e10dd2dd33924ddf6d7d260744c9df4344c3ac39d9ba438a5ddcbf13}.
The compact archive also retains the dirty-worktree patch and the runner and
validator used for this matrix.

\section*{Completion and Timing}

All eight configurations completed all 80 time steps with successful nonlinear
termination.  Each run contains 81 finite response states and 81 visualization
cycles.  Times below are challenge-suite max-rank
wall times and therefore include field output.  The iteration column is the sum
of the terminal nonlinear iteration index over all time steps.

\begin{table}[h]
  \centering
  \small
  \caption{Viscoelastic completion, nonlinear work, and max-rank wall time.}
  \begin{tabular}{@{}lrrrr@{}}
    \toprule
    & \multicolumn{2}{c}{Hypre AMG} & \multicolumn{2}{c}{Hypre Jacobi} \\
    \cmidrule(lr){2-3}\cmidrule(l){4-5}
    Solver & Iterations & Time (s) & Iterations & Time (s) \\
    \midrule
    Newton-LS & 3118 & 417.741 & 3020 & 762.974 \\
    Newton-CP & 475 & 51.389 & 475 & 97.628 \\
    Trust-region & 510 & 35.636 & 510 & 64.502 \\
    Trust-region + subspace & 508 & 37.407 & 506 & 62.805 \\
    \bottomrule
  \end{tabular}
\end{table}

Baseline trust region with Hypre AMG is the fastest configuration.  AMG is
faster than Jacobi for all four nonlinear methods. Newton-CP uses slightly fewer
nonlinear iterations than the trust-region methods, while Newton-LS requires
more than 3000 because the snap increment takes 2467 iterations with AMG and
2369 with Jacobi. These timings include all 81 field writes and therefore
compare the full evidence-producing workflow.

\section*{Response and Recovery Validation}

All eight response histories are identical in stored displacement and prescribed
force, with a largest pointwise bottom-reaction spread of
$6.20\times10^{-12}$. The average top displacement jumps from $-0.0192461$ at
load 0.21 to $-0.158163$ at load 0.24. At $t=6$, every run reaches average top
displacement $-0.569260$, applied force 0.6, and balancing bottom reaction 0.6.
When the force is removed at $t=6.3$, the cap rebounds to displacement
$-0.440359$ and the globally reduced vertical reaction returns to numerical
zero.

During the remaining accepted states, the displacement relaxes monotonically
toward zero.  Every run finishes at average top displacement $-0.0307083$.
The largest reaction magnitude at any unloaded state is $6.00\times10^{-12}$.
The reaction is computed by mapping boundary vector
degrees of freedom to locally owned true degrees of freedom, summing each owned
entry once, and applying one global reduction.  The earlier implementation
summed local vector degrees of freedom directly and reduced that result again at
the viscoelastic call site.  Its nonzero unloaded value was therefore not a
valid global support reaction and is superseded by this matrix.

\begin{figure}[h]
  \centering
  \includegraphics[width=\textwidth]{/usr/workspace/tupek2/dev/buckling_solver_paper/figures/viscoelastic_solver_response.png}
  \caption{Current matched viscoelastic buckling and recovery response. The
  displacement jump occurs between loads 0.21 and 0.24; the dotted line marks
  the force maximum at $t=6$, and unloading begins at $t=6.3$.}
\end{figure}

\section*{Solver Diagnostics and Interpretation}

No configuration fails or selects a distinct response branch. The displacement
histories are identical at stored precision, and representative AMG field states
agree to within $3\times10^{-13}$ in every displacement component. The case
therefore demonstrates that approximate energetic acceptance reproduces the
same buckling, unloading, and recovery path as both Newton variants for this
path-dependent material. It also exposes a nonlinear-work separation:
Newton-LS needs more than 3000 iterations, whereas Newton-CP and both
trust-region methods need approximately 500.

The baseline and subspace trust-region results remain close in nonlinear work.
Their small timing differences should be treated as case-specific measurements
rather than evidence of a general advantage.

\section*{Deformation Comparison}

The September 17 full-output matrix contains all 81 field cycles. The comparison
shows the adjacent pre- and post-snap states together with the initial, peak,
first unloaded, and final recovered states.

\begin{figure}[h]
  \centering
  \includegraphics[width=\textwidth]{/usr/workspace/tupek2/dev/buckling_solver_paper/figures/viscoelastic_initial_peak_final.png}
  \caption{Viscoelastic buckling and recovery for subspace trust region with
  Hypre AMG, shown at physical deformation scale without displacement
  amplification. The common color range spans all six states.}
\end{figure}

\end{document}
